import 'package:flutter/material.dart';

class Nothingscreen extends StatelessWidget {
  const Nothingscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ok biee...")),
    );
  }
}
